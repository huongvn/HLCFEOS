# BMS Engine - Credentials (NOT committed to git)
# Copy to .env on the board (or set in systemd EnvironmentFile).
# These override the (empty) user/pass fields in config/config.yaml.

# Local NanoMQ broker (leave empty for anonymous)
BMS_MQTT_USER=
BMS_MQTT_PASS=

# Remote xsolar broker
BMS_XSOLAR_USER=admin
BMS_XSOLAR_PASS=xSoLar@123234@sb
